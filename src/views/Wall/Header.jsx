import { Box, Heading, Text } from '@chakra-ui/layout';
import React, { memo } from 'react';

export default memo(() => (
	<Box>
		<Heading>Walls</Heading>
		<Text as="h3">See what people are talking</Text>
	</Box>
));
